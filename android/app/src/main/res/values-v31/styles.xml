<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Android 12+ (API 31) owns the splash screen itself via the platform SplashScreen
         API and ignores the legacy @drawable/splash windowBackground approach — without
         this override it shows a plain white flash before the app takes over. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
