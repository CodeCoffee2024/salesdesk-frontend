import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.salesdesk.app',
  appName: 'SalesDesk',
  webDir: 'dist/sales-desk.front-end'
};

export default config;
